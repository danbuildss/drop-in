# Project Rules

Always add explicit TypeScript types to:
* Component props
* Function parameters
* Empty arrays in useState
* Object key lookups

Never leave implicit `any`.

Before finishing any task, verify with `npm run build`.
